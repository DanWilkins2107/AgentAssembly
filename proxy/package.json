{
  "name": "agentassembly-proxy",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "bin": {
    "agentassembly-proxy": "./dist/main.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --coverage",
    "mutation": "stryker run",
    "lint": "eslint .",
    "format:check": "prettier --check ."
  }
}
