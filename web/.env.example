# === Public tier === (see docs/secrets.md)
# These values are PUBLIC BY DESIGN — safe in a browser bundle and safe to commit
# as placeholders. Row Level Security is the real gate, not the secrecy of these.
# Copy this file to `web/.env` (gitignored) and fill in your project's values.
VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_ANON_KEY=your-anon-key
